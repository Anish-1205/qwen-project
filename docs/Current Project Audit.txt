# Current Project Audit

Last verified: 20 August 2026.

The canonical, fully updated audit is:

    docs/PROJECT_AUDIT_FULL.md

The operational handoff is:

    docs/PROJECT_HANDOFF.md

This file is intentionally a short index so that two long audit documents do not drift independently.

Current headline status:

- Tool calling is integrated into the production ConversationOrchestrator.
- Approved tools are calculator, roll_die, and random_number.
- Production tool arguments are allowlisted and validated; the calculator uses a restricted AST evaluator rather than eval().
- The historical prototype remains under experiments/ and is not production code.
- The discoverable unittest suite contains 59 tests; 59/59 passed on 20 August 2026.
- Live read-only database snapshot: 6 memory archive rows (4 active), 4 cache rows, 9 indexed documents/9 chunks, and 1 web session/18 messages.
- The old duplicate-active-memory defect is fixed in current code and not present in active live relations.
- No dependency manifest exists.
- Current top risks are web reset persistence semantics, sensitive unrotated logs, missing upload size/type validation, and lack of an exact-revision end-to-end real-Qwen regression.

The workspace is not currently a Git repository, so there is no commit or branch identifier for this audit.
